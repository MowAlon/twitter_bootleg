<div class="container">
  <div class="row">
    <div class="col-md-offset-1">
      <% @tweets.each do |tweet| %>
        <div class="panel">@username: <%= tweet %></div>
      <% end %>
    </div>
  </div>
</div>
